/** JSON-LD schema builders — canonical implementation lives in lib/schema.ts.
 *  Re-exported here to satisfy the documented seo/ folder architecture. */
export {
  dentistSchema,
  organizationSchema,
  websiteSchema,
  faqSchema,
  servicesSchema,
  breadcrumbSchema,
} from "@/lib/schema";
