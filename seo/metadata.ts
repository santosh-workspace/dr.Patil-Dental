/** SEO metadata builder — canonical implementation lives in lib/metadata.ts.
 *  Re-exported here to satisfy the documented seo/ folder architecture. */
export { buildMetadata } from "@/lib/metadata";
